import { adminAuthRegister } from './auth.js';
import { clear } from './other.js'

//BEGINNING OF AUTH REGISTER TESTING
test('Checking a successful registration', () => {
    clear();

});

test('Checking if email is used by other user', () => {
    clear();



});

test('Email satisfies the structure and condition', () => {
    clear();



});

test('Checking Password length is at least 8', () => {
    clear();



});

test('Checking if Password contains at least one letter and number', () => {
    clear();



});

test('Is nameFirst contains abnormal characters', () => {
    clear();



});

test('nameLast contains abnormal characters', () => {
    clear();



});

test('NameFirst not less than 2 characters and not greater than 20', () => {
    clear();



});

test('NameLast not less than 2 characters and not greater than 20', () => {
    clear();



});

//END OF AUTH REGISTER TESTING